 R←Test_UC_002(stopFlag batchFlag);⎕TRAP;list;list2
⍝ Checks whether there are are two conflicts which are identical and two that aren't. The idenntical files are deleted.
 ⎕TRAP←(999 'C' '∘∘∘ ⍝ Deliberate error')(0 'N')

 R←T._Failed

 ⎕SE.Dropbox.CommTools.AddAutomation'YesOrNo' 'ExecInRoot@' 'y'
 list←⎕SE.UCMD'Dropbox ',∆PATH
 →T.GoToTidyUp 2≠≢list
 list2←⎕SE.Dropbox.ListAllConflictingFiles ∆PATH
 →T.GoToTidyUp 2≠≢list2    ⍝ Because the two that are identical got deleted!

 R←T._OK

∆TidyUp:
 ⎕SE.Dropbox.CommTools.Cleanup
⍝Done
